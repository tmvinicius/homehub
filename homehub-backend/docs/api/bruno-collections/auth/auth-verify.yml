info:
  name: Verify HomeHub
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/auth/verify
  headers:
    - name: Authorization
      value: "{{JWT}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
