info:
  name: Login HomeHub
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/auth/login
  body:
    type: json
    data: |-
      {
        "email":"admin@homehub.com",
        "password":"tmV123456@"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
