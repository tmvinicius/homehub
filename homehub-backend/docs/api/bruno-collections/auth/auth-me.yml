info:
  name: Me HomeHub
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/auth/me
  headers:
    - name: Authorization
      value: "{{JWT}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
